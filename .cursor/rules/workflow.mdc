---
description: Autonomous execution — user assigns tasks and corrections only.
alwaysApply: true
---

# Режим работы в Nova

- Доводи задачи до конца сам: код, проверка (`npm run build`), коммит, `git push` (деплой Vercel с `main`).
- Не спрашивай разрешения на очевидные следующие шаги (деплой после правок сайта, исправление сборки).
- Пользователь даёт задания и корректировки — кратко отчитывайся: что сделано, где смотреть (локально / прод URL).
- Продакшен (работает сейчас): https://nova-muse.vercel.app — деплой: `npx vercel deploy --prod` из корня Nova.
- Свой домен https://nova-versel-ai.app — только после DNS: A-запись `76.76.21.21` у регистратора (см. Vercel → nova → Domains).
- Старый preview URL `nova-gfx879g4j-…` не обновляется — не использовать.
- Не коммить `.env`, секреты, токены. `.vscode/` — только по запросу.
